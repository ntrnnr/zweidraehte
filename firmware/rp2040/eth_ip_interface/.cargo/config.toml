[target.'cfg(all(target_arch = "arm", target_os = "none"))']
# TODO: update probe serial to match the target Pico's debug probe.
runner = "probe-rs run --chip RP2040 --no-catch-reset"

[build]
target = "thumbv6m-none-eabi"
