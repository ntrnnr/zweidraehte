target/
out/
.claude/

*.log
.DS_Store

# Local RSA converter signing key (never commit — see LICENSING.md / keys.rs)
converter_key.xml

# Host-target device runtime state (per-run; regenerated on first launch).
# Written by the firmware/linux/* device shells next to their binary.
*_device_state.json
device_identity.json
secure_device_identity.json
*_seq.bin
